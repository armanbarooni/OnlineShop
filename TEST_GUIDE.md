# گزارش تست کامل سیستم OnlineShop

## اطلاعات تست
- **تاریخ**: 2025-12-06
- **شماره تست**: 09168044154
- **محیط**: Development (localhost:8080 + localhost:5000)

---

## ✅ مراحل تست ثبت‌نام و ورود

### 1. تست صفحه ثبت‌نام (Register)
**URL**: http://localhost:8080/register.html

#### مراحل تست:
1. باز کردن صفحه ثبت‌نام
2. پر کردن فرم مرحله 1 (اطلاعات شخصی):
   - نام: [نام تست]
   - نام خانوادگی: [نام خانوادگی تست]
   - شماره موبایل: 09168044154
   - ایمیل: test@example.com

3. کلیک روی دکمه "مرحله بعد"
4. پر کردن مرحله 2 (تأیید شماره):
   - وارد کردن کد OTP که به شماره 09168044154 ارسال می‌شود
   
5. پر کردن مرحله 3 (رمز عبور):
   - رمز عبور: Test@123456
   - تکرار رمز عبور: Test@123456
   
6. پر کردن مرحله 4 (آدرس):
   - آدرس کامل
   - شهر
   - کد پستی

#### نکات بررسی:
- [ ] آیا فرم به درستی نمایش داده می‌شود؟
- [ ] آیا اعتبارسنجی فیلدها کار می‌کند؟
- [ ] آیا پیام خطا به فارسی نمایش داده می‌شود؟
- [ ] آیا کد OTP ارسال می‌شود؟
- [ ] آیا پس از ثبت‌نام موفق به صفحه لاگین هدایت می‌شود؟

---

### 2. تست صفحه ورود (Login)
**URL**: http://localhost:8080/login.html

#### مراحل تست:
1. باز کردن صفحه ورود
2. انتخاب روش ورود:
   - با رمز عبور
   - با کد یکبار مصرف (OTP)

#### تست ورود با رمز عبور:
- شماره موبایل: 09168044154
- رمز عبور: Test@123456
- کلیک روی "ورود"

#### تست ورود با OTP:
- شماره موبایل: 09168044154
- دریافت کد
- وارد کردن کد OTP
- کلیک روی "ورود"

#### نکات بررسی:
- [ ] آیا هر دو روش ورود کار می‌کنند؟
- [ ] آیا پس از ورود موفق به پنل کاربری هدایت می‌شود؟
- [ ] آیا توکن احراز هویت ذخیره می‌شود؟
- [ ] آیا پیام‌های خطا به درستی نمایش داده می‌شوند؟

---

## 🐛 تست باگ‌های گزارش شده در پنل کاربری

### باگ 1: صفحه جزئیات سفارش (Order Detail)
**URL**: http://localhost:8080/user-panel-order-detail.html

#### مشکل گزارش شده:
"صفحه باز می‌شود اما می‌پرد و نمی‌گذارد محتوا را ببینم"

#### مراحل تست:
1. ورود به سیستم
2. رفتن به "سفارشات من"
3. کلیک روی یک سفارش برای مشاهده جزئیات

#### چک‌لیست بررسی:
- [ ] آیا صفحه به درستی بارگذاری می‌شود؟
- [ ] آیا اسکرول صفحه کار می‌کند؟
- [ ] آیا محتوا قابل مشاهده است؟
- [ ] آیا خطایی در Console مرورگر وجود دارد؟
- [ ] آیا JavaScript error وجود دارد؟

**راه حل پیشنهادی**:
- بررسی کنسول مرورگر (F12)
- چک کردن CSS conflicts
- بررسی JavaScript errors

---

### باگ 2: صفحه ویرایش آدرس (Edit Address)
**URL**: http://localhost:8080/user-panel-edit-address.html

#### مشکل گزارش شده:
"صفحه باز نمی‌شود"

#### مراحل تست:
1. ورود به سیستم
2. رفتن به "آدرس‌های من"
3. کلیک روی دکمه "ویرایش" یک آدرس

#### چک‌لیست بررسی:
- [ ] آیا صفحه باز می‌شود؟
- [ ] آیا فرم ویرایش نمایش داده می‌شود؟
- [ ] آیا داده‌های آدرس بارگذاری می‌شوند؟
- [ ] آیا نقشه نمایش داده می‌شود؟
- [ ] آیا دکمه ذخیره کار می‌کند؟

---

### باگ 3: صفحه لیست آدرس‌ها (Address List)
**URL**: http://localhost:8080/user-panel-address.html

#### مشکلات گزارش شده:
1. خطای ناشناخته در گوشه بالا راست
2. منوی dropdown بالا سمت چپ کار نمی‌کند
3. آیکون پروفایل redirect نمی‌کند
4. دکمه‌های ویرایش و حذف کار نمی‌کنند (x3 آدرس)

#### مراحل تست:
1. ورود به سیستم
2. رفتن به "آدرس‌های من"

#### چک‌لیست بررسی:
- [ ] آیا لیست آدرس‌ها نمایش داده می‌شود؟
- [ ] آیا دکمه "افزودن آدرس جدید" کار می‌کند؟
- [ ] آیا دکمه‌های ویرایش کار می‌کنند؟
- [ ] آیا دکمه‌های حذف کار می‌کنند؟
- [ ] آیا دکمه "تنظیم به عنوان پیش‌فرض" کار می‌کند؟
- [ ] آیا منوی کاربر در بالا کار می‌کند؟

---

### باگ 4: صفحه کیف پول (Wallet)
**URL**: http://localhost:8080/user-panel-wallet.html

#### مشکل گزارش شده:
"موجودی و تراکنش‌ها نمایش داده نمی‌شوند"

#### مراحل تست:
1. ورود به سیستم
2. رفتن به "کیف پول"

#### چک‌لیست بررسی:
- [ ] آیا موجودی فعلی نمایش داده می‌شود؟
- [ ] آیا لیست تراکنش‌ها بارگذاری می‌شود؟
- [ ] آیا دکمه "افزایش موجودی" کار می‌کند؟
- [ ] آیا دکمه "انتقال وجه" کار می‌کند؟

---

### باگ 5: صفحه تیکت‌ها (Tickets)
**URL**: http://localhost:8080/user-panel-ticket.html

#### مشکل گزارش شده:
"لیست تیکت‌ها نمایش داده نمی‌شود"

#### مراحل تست:
1. ورود به سیستم
2. رفتن به "تیکت‌های من"

#### چک‌لیست بررسی:
- [ ] آیا لیست تیکت‌ها نمایش داده می‌شود؟
- [ ] آیا دکمه "ارسال تیکت جدید" کار می‌کند؟
- [ ] آیا می‌توان روی یک تیکت کلیک کرد و چت را باز کرد؟

---

### باگ 6: صفحه دریافت تخفیف (Get Discount)
**URL**: http://localhost:8080/user-panel-get-discount.html

#### مشکل گزارش شده:
"کد تخفیف اعمال نمی‌شود و خطا می‌دهد"

#### مراحل تست:
1. ورود به سیستم
2. رفتن به "دریافت تخفیف"
3. وارد کردن کد تخفیف
4. کلیک روی "اعمال کد"

#### چک‌لیست بررسی:
- [ ] آیا فرم نمایش داده می‌شود؟
- [ ] آیا کد تخفیف اعتبارسنجی می‌شود؟
- [ ] آیا پیام موفقیت/خطا نمایش داده می‌شود؟

---

### باگ 7: صفحه اعلان‌های سایت (Site Notifications)
**URL**: http://localhost:8080/user-panel-site-notification.html

#### مشکل گزارش شده:
"اعلان‌ها نمایش داده نمی‌شوند"

#### مراحل تست:
1. ورود به سیستم
2. رفتن به "اعلان‌های سایت"

#### چک‌لیست بررسی:
- [ ] آیا لیست اعلان‌ها بارگذاری می‌شود؟
- [ ] آیا می‌توان اعلان را به عنوان خوانده شده علامت زد؟
- [ ] آیا می‌توان اعلان را حذف کرد؟

---

### باگ 8: صفحه آخرین بازدیدها (Last Viewed)
**URL**: http://localhost:8080/user-panel-last-viewd.html

#### مشکل گزارش شده:
"محصولات بازدید شده نمایش داده نمی‌شوند"

#### مراحل تست:
1. ورود به سیستم
2. بازدید از چند محصول
3. رفتن به "آخرین بازدیدها"

#### چک‌لیست بررسی:
- [ ] آیا لیست محصولات بازدید شده نمایش داده می‌شود؟
- [ ] آیا تصاویر محصولات بارگذاری می‌شوند؟
- [ ] آیا لینک‌ها به صفحه محصول کار می‌کنند؟

---

### باگ 9: صفحه فعالیت‌ها (Activities)
**URL**: http://localhost:8080/user-panel-activiti.html

#### مشکل گزارش شده:
"فعالیت‌ها نمایش داده نمی‌شوند"

#### مراحل تست:
1. ورود به سیستم
2. رفتن به "فعالیت‌ها"

#### چک‌لیست بررسی:
- [ ] آیا لیست فعالیت‌ها بارگذاری می‌شود؟
- [ ] آیا تاریخ و زمان به درستی نمایش داده می‌شود؟

---

## 📋 چک‌لیست عمومی برای همه صفحات

### بررسی‌های کلی:
- [ ] آیا صفحه به درستی بارگذاری می‌شود؟
- [ ] آیا متن‌های فارسی به درستی نمایش داده می‌شوند؟
- [ ] آیا راست‌چین (RTL) به درستی کار می‌کند؟
- [ ] آیا تم تاریک/روشن کار می‌کند؟
- [ ] آیا صفحه responsive است؟
- [ ] آیا خطایی در Console وجود دارد؟
- [ ] آیا درخواست‌های API موفق هستند؟
- [ ] آیا پیام‌های خطا قابل فهم هستند؟

### بررسی Network:
- [ ] بررسی تب Network در DevTools
- [ ] چک کردن status code های API (باید 200 باشد)
- [ ] بررسی زمان پاسخ API
- [ ] چک کردن payload های ارسالی و دریافتی

### بررسی Console:
- [ ] باز کردن Console (F12)
- [ ] جستجوی خطاهای JavaScript
- [ ] بررسی warning ها
- [ ] چک کردن لاگ‌های API

---

## 🔧 راهنمای رفع مشکلات رایج

### مشکل: صفحه باز نمی‌شود
**راه حل**:
1. چک کنید backend در حال اجرا است
2. بررسی کنید config.js به درستی تنظیم شده
3. چک کنید CORS به درستی پیکربندی شده

### مشکل: API خطا می‌دهد
**راه حل**:
1. بررسی کنسول backend
2. چک کردن database connection
3. بررسی JWT token

### مشکل: داده‌ها نمایش داده نمی‌شوند
**راه حل**:
1. چک کردن Network tab
2. بررسی response API
3. چک کردن JavaScript errors

---

## 📊 فرم گزارش باگ

برای هر باگ پیدا شده، لطفاً موارد زیر را ثبت کنید:

```
عنوان باگ: [عنوان کوتاه]
صفحه: [URL صفحه]
مراحل بازتولید:
1. 
2. 
3. 

نتیجه مورد انتظار:
[چه اتفاقی باید بیفتد]

نتیجه واقعی:
[چه اتفاقی افتاد]

خطای Console:
[کپی خطا از Console]

Screenshot:
[اسکرین‌شات مشکل]

اولویت: [بحرانی / بالا / متوسط / پایین]
```

---

## ✅ نتیجه نهایی

پس از تکمیل تست‌ها، لطفاً موارد زیر را گزارش دهید:
- تعداد کل باگ‌های پیدا شده
- تعداد باگ‌های بحرانی
- تعداد باگ‌های رفع شده
- باگ‌های باقی‌مانده

---

**تاریخ تکمیل تست**: _______________
**تست شده توسط**: _______________
**وضعیت کلی**: [ ] موفق  [ ] ناموفق  [ ] نیاز به بررسی بیشتر
